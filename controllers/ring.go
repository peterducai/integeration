//If a ring R has a multiplicative
//unit element 1 and every nonzero element has a multiplicative
//inverse, then R is called a division ring.

//Every finite division ring is commutative

//Every finite division ring is a field TODO: check

package controllers

import (
	"fmt"
)

func createRing() {
	fmt.Println("creating ring")
}

//isCommutative check if ring is commutative TODO: explain
func isCommutative() bool {
	return true
}
